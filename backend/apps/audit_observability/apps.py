from django.apps import AppConfig


class AuditObservabilityConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.audit_observability"
    verbose_name = "Audit & Observability"
